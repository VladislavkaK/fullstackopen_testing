import { createGlobalStyle } from 'styled-components';

const GlobalContainer = createGlobalStyle`
    body {
        margin: 0;
        padding: 0;
        height: 100vh;
    }
`;

export {
    GlobalContainer
}